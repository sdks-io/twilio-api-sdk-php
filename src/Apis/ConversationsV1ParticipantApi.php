<?php

declare(strict_types=1);

/*
 * TwilioApIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace TwilioApIsLib\Apis;

use Core\Request\Parameters\FormParam;
use Core\Request\Parameters\HeaderParam;
use Core\Request\Parameters\QueryParam;
use Core\Request\Parameters\TemplateParam;
use CoreInterfaces\Core\Request\RequestMethod;
use TwilioApIsLib\Http\ApiResponse;
use TwilioApIsLib\Models\ChannelWebhookEnabledType;
use TwilioApIsLib\Models\ConversationParticipant;
use TwilioApIsLib\Models\ListConversationParticipantResponse;
use TwilioApIsLib\Models\ListServiceConversationParticipantResponse;
use TwilioApIsLib\Models\ServiceConversationParticipant;
use TwilioApIsLib\Server;
use TwilioApIsLib\Utils\DateTimeHelper;

class ConversationsV1ParticipantApi extends BaseApi
{
    /**
     * Add a new participant to the conversation
     *
     * @param string $conversationSid The unique ID of the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for this participant.
     * @param string|null $xTwilioWebhookEnabled The X-Twilio-Webhook-Enabled HTTP request header
     * @param string|null $identity A unique string identifier for the conversation participant as
     *        [Conversation User](https://www.twilio.com/docs/conversations/api/user-resource).
     *        This parameter is non-null if (and only if) the participant is using the
     *        Conversations SDK to communicate. Limited to 256 characters.
     * @param string|null $messagingBindingAddress The address of the participant's device, e.g. a
     *        phone or WhatsApp number. Together with the Proxy address, this determines a
     *        participant uniquely. This field (with proxy_address) is only null when the
     *        participant is interacting from an SDK endpoint (see the 'identity' field).
     * @param string|null $messagingBindingProxyAddress The address of the Twilio phone number (or
     *        WhatsApp number) that the participant is in contact with. This field, together with
     *        participant address, is only null when the participant is interacting from an SDK
     *        endpoint (see the 'identity' field).
     * @param \DateTime|null $dateCreated The date that this resource was created.
     * @param \DateTime|null $dateUpdated The date that this resource was last updated.
     * @param string|null $attributes An optional string metadata field you can use to store any
     *        data you wish. The string value must contain structurally valid JSON if specified.
     *        **Note** that if the attributes are not set "{}" will be returned.
     * @param string|null $messagingBindingProjectedAddress The address of the Twilio phone number
     *        that is used in Group MMS. Communication mask for the Conversation participant with
     *        Identity.
     * @param string|null $roleSid The SID of a conversation-level
     *        [Role](https://www.twilio.com/docs/conversations/api/role-resource) to assign to the
     *        participant.
     *
     * @return ApiResponse Response from the API call
     */
    public function createConversationParticipant(
        string $conversationSid,
        ?string $xTwilioWebhookEnabled = null,
        ?string $identity = null,
        ?string $messagingBindingAddress = null,
        ?string $messagingBindingProxyAddress = null,
        ?\DateTime $dateCreated = null,
        ?\DateTime $dateUpdated = null,
        ?string $attributes = null,
        ?string $messagingBindingProjectedAddress = null,
        ?string $roleSid = null
    ): ApiResponse {
        $_reqBuilder = $this->requestBuilder(
            RequestMethod::POST,
            '/v1/Conversations/{ConversationSid}/Participants'
        )
            ->server(Server::DEFAULT2)
            ->auth('accountSid_authToken')
            ->parameters(
                TemplateParam::init('ConversationSid', $conversationSid)->required(),
                HeaderParam::init('Content-Type', 'application/x-www-form-urlencoded'),
                HeaderParam::init('X-Twilio-Webhook-Enabled', $xTwilioWebhookEnabled)
                    ->serializeBy([ChannelWebhookEnabledType::class, 'checkValue']),
                FormParam::init('Identity', $identity),
                FormParam::init('MessagingBinding.Address', $messagingBindingAddress),
                FormParam::init('MessagingBinding.ProxyAddress', $messagingBindingProxyAddress),
                FormParam::init('DateCreated', $dateCreated)
                    ->serializeBy([DateTimeHelper::class, 'toRfc3339DateTime']),
                FormParam::init('DateUpdated', $dateUpdated)
                    ->serializeBy([DateTimeHelper::class, 'toRfc3339DateTime']),
                FormParam::init('Attributes', $attributes),
                FormParam::init('MessagingBinding.ProjectedAddress', $messagingBindingProjectedAddress),
                FormParam::init('RoleSid', $roleSid)
            );

        $_resHandler = $this->responseHandler()->type(ConversationParticipant::class)->returnApiResponse();

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Retrieve a list of all participants of the conversation
     *
     * @param string $conversationSid The unique ID of the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for participants.
     * @param int|null $pageSize How many resources to return in each list page. The default is 50,
     *        and the maximum is 100.
     * @param int|null $page The page index. This value is simply for client state.
     * @param string|null $pageToken The page token. This is provided by the API.
     *
     * @return ApiResponse Response from the API call
     */
    public function listConversationParticipant(
        string $conversationSid,
        ?int $pageSize = null,
        ?int $page = null,
        ?string $pageToken = null
    ): ApiResponse {
        $_reqBuilder = $this->requestBuilder(
            RequestMethod::GET,
            '/v1/Conversations/{ConversationSid}/Participants'
        )
            ->server(Server::DEFAULT2)
            ->auth('accountSid_authToken')
            ->parameters(
                TemplateParam::init('ConversationSid', $conversationSid)->required(),
                QueryParam::init('PageSize', $pageSize),
                QueryParam::init('Page', $page),
                QueryParam::init('PageToken', $pageToken)
            );

        $_resHandler = $this->responseHandler()->type(ListConversationParticipantResponse::class)->returnApiResponse();

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Update an existing participant in the conversation
     *
     * @param string $conversationSid The unique ID of the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for this participant.
     * @param string $sid A 34 character string that uniquely identifies this resource.
     * @param string|null $xTwilioWebhookEnabled The X-Twilio-Webhook-Enabled HTTP request header
     * @param \DateTime|null $dateCreated The date that this resource was created.
     * @param \DateTime|null $dateUpdated The date that this resource was last updated.
     * @param string|null $attributes An optional string metadata field you can use to store any
     *        data you wish. The string value must contain structurally valid JSON if specified.
     *        **Note** that if the attributes are not set "{}" will be returned.
     * @param string|null $roleSid The SID of a conversation-level
     *        [Role](https://www.twilio.com/docs/conversations/api/role-resource) to assign to the
     *        participant.
     * @param string|null $messagingBindingProxyAddress The address of the Twilio phone number that
     *        the participant is in contact with. 'null' value will remove it.
     * @param string|null $messagingBindingProjectedAddress The address of the Twilio phone number
     *        that is used in Group MMS. 'null' value will remove it.
     * @param string|null $identity A unique string identifier for the conversation participant as
     *        [Conversation User](https://www.twilio.com/docs/conversations/api/user-resource).
     *        This parameter is non-null if (and only if) the participant is using the
     *        Conversations SDK to communicate. Limited to 256 characters.
     * @param int|null $lastReadMessageIndex Index of last “read” message in the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for the Participant.
     * @param string|null $lastReadTimestamp Timestamp of last “read” message in the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for the Participant.
     *
     * @return ApiResponse Response from the API call
     */
    public function updateConversationParticipant(
        string $conversationSid,
        string $sid,
        ?string $xTwilioWebhookEnabled = null,
        ?\DateTime $dateCreated = null,
        ?\DateTime $dateUpdated = null,
        ?string $attributes = null,
        ?string $roleSid = null,
        ?string $messagingBindingProxyAddress = null,
        ?string $messagingBindingProjectedAddress = null,
        ?string $identity = null,
        ?int $lastReadMessageIndex = null,
        ?string $lastReadTimestamp = null
    ): ApiResponse {
        $_reqBuilder = $this->requestBuilder(
            RequestMethod::POST,
            '/v1/Conversations/{ConversationSid}/Participants/{Sid}'
        )
            ->server(Server::DEFAULT2)
            ->auth('accountSid_authToken')
            ->parameters(
                TemplateParam::init('ConversationSid', $conversationSid)->required(),
                TemplateParam::init('Sid', $sid)->required(),
                HeaderParam::init('Content-Type', 'application/x-www-form-urlencoded'),
                HeaderParam::init('X-Twilio-Webhook-Enabled', $xTwilioWebhookEnabled)
                    ->serializeBy([ChannelWebhookEnabledType::class, 'checkValue']),
                FormParam::init('DateCreated', $dateCreated)
                    ->serializeBy([DateTimeHelper::class, 'toRfc3339DateTime']),
                FormParam::init('DateUpdated', $dateUpdated)
                    ->serializeBy([DateTimeHelper::class, 'toRfc3339DateTime']),
                FormParam::init('Attributes', $attributes),
                FormParam::init('RoleSid', $roleSid),
                FormParam::init('MessagingBinding.ProxyAddress', $messagingBindingProxyAddress),
                FormParam::init('MessagingBinding.ProjectedAddress', $messagingBindingProjectedAddress),
                FormParam::init('Identity', $identity),
                FormParam::init('LastReadMessageIndex', $lastReadMessageIndex),
                FormParam::init('LastReadTimestamp', $lastReadTimestamp)
            );

        $_resHandler = $this->responseHandler()->type(ConversationParticipant::class)->returnApiResponse();

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Remove a participant from the conversation
     *
     * @param string $conversationSid The unique ID of the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for this participant.
     * @param string $sid A 34 character string that uniquely identifies this resource.
     * @param string|null $xTwilioWebhookEnabled The X-Twilio-Webhook-Enabled HTTP request header
     *
     * @return ApiResponse Response from the API call
     */
    public function deleteConversationParticipant(
        string $conversationSid,
        string $sid,
        ?string $xTwilioWebhookEnabled = null
    ): ApiResponse {
        $_reqBuilder = $this->requestBuilder(
            RequestMethod::DELETE,
            '/v1/Conversations/{ConversationSid}/Participants/{Sid}'
        )
            ->server(Server::DEFAULT2)
            ->auth('accountSid_authToken')
            ->parameters(
                TemplateParam::init('ConversationSid', $conversationSid)->required(),
                TemplateParam::init('Sid', $sid)->required(),
                HeaderParam::init('X-Twilio-Webhook-Enabled', $xTwilioWebhookEnabled)
                    ->serializeBy([ChannelWebhookEnabledType::class, 'checkValue'])
            );

        $_resHandler = $this->responseHandler()->returnApiResponse();

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Fetch a participant of the conversation
     *
     * @param string $conversationSid The unique ID of the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for this participant.
     * @param string $sid A 34 character string that uniquely identifies this resource.
     *        Alternatively, you can pass a Participant's `identity` rather than the SID.
     *
     * @return ApiResponse Response from the API call
     */
    public function fetchConversationParticipant(string $conversationSid, string $sid): ApiResponse
    {
        $_reqBuilder = $this->requestBuilder(
            RequestMethod::GET,
            '/v1/Conversations/{ConversationSid}/Participants/{Sid}'
        )
            ->server(Server::DEFAULT2)
            ->auth('accountSid_authToken')
            ->parameters(
                TemplateParam::init('ConversationSid', $conversationSid)->required(),
                TemplateParam::init('Sid', $sid)->required()
            );

        $_resHandler = $this->responseHandler()->type(ConversationParticipant::class)->returnApiResponse();

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Add a new participant to the conversation in a specific service
     *
     * @param string $chatServiceSid The SID of the [Conversation
     *        Service](https://www.twilio.com/docs/conversations/api/service-resource) the
     *        Participant resource is associated with.
     * @param string $conversationSid The unique ID of the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for this participant.
     * @param string|null $xTwilioWebhookEnabled The X-Twilio-Webhook-Enabled HTTP request header
     * @param string|null $identity A unique string identifier for the conversation participant as
     *        [Conversation User](https://www.twilio.com/docs/conversations/api/user-resource).
     *        This parameter is non-null if (and only if) the participant is using the
     *        [Conversation SDK](https://www.twilio.com/docs/conversations/sdk-overview) to
     *        communicate. Limited to 256 characters.
     * @param string|null $messagingBindingAddress The address of the participant's device, e.g. a
     *        phone or WhatsApp number. Together with the Proxy address, this determines a
     *        participant uniquely. This field (with `proxy_address`) is only null when the
     *        participant is interacting from an SDK endpoint (see the `identity` field).
     * @param string|null $messagingBindingProxyAddress The address of the Twilio phone number (or
     *        WhatsApp number) that the participant is in contact with. This field, together with
     *        participant address, is only null when the participant is interacting from an SDK
     *        endpoint (see the `identity` field).
     * @param \DateTime|null $dateCreated The date on which this resource was created.
     * @param \DateTime|null $dateUpdated The date on which this resource was last updated.
     * @param string|null $attributes An optional string metadata field you can use to store any
     *        data you wish. The string value must contain structurally valid JSON if specified.
     *        **Note** that if the attributes are not set `{}` will be returned.
     * @param string|null $messagingBindingProjectedAddress The address of the Twilio phone number
     *        that is used in Group MMS.
     * @param string|null $roleSid The SID of a conversation-level
     *        [Role](https://www.twilio.com/docs/conversations/api/role-resource) to assign to the
     *        participant.
     *
     * @return ApiResponse Response from the API call
     */
    public function createServiceConversationParticipant(
        string $chatServiceSid,
        string $conversationSid,
        ?string $xTwilioWebhookEnabled = null,
        ?string $identity = null,
        ?string $messagingBindingAddress = null,
        ?string $messagingBindingProxyAddress = null,
        ?\DateTime $dateCreated = null,
        ?\DateTime $dateUpdated = null,
        ?string $attributes = null,
        ?string $messagingBindingProjectedAddress = null,
        ?string $roleSid = null
    ): ApiResponse {
        $_reqBuilder = $this->requestBuilder(
            RequestMethod::POST,
            '/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants'
        )
            ->server(Server::DEFAULT2)
            ->auth('accountSid_authToken')
            ->parameters(
                TemplateParam::init('ChatServiceSid', $chatServiceSid)->required(),
                TemplateParam::init('ConversationSid', $conversationSid)->required(),
                HeaderParam::init('Content-Type', 'application/x-www-form-urlencoded'),
                HeaderParam::init('X-Twilio-Webhook-Enabled', $xTwilioWebhookEnabled)
                    ->serializeBy([ChannelWebhookEnabledType::class, 'checkValue']),
                FormParam::init('Identity', $identity),
                FormParam::init('MessagingBinding.Address', $messagingBindingAddress),
                FormParam::init('MessagingBinding.ProxyAddress', $messagingBindingProxyAddress),
                FormParam::init('DateCreated', $dateCreated)
                    ->serializeBy([DateTimeHelper::class, 'toRfc3339DateTime']),
                FormParam::init('DateUpdated', $dateUpdated)
                    ->serializeBy([DateTimeHelper::class, 'toRfc3339DateTime']),
                FormParam::init('Attributes', $attributes),
                FormParam::init('MessagingBinding.ProjectedAddress', $messagingBindingProjectedAddress),
                FormParam::init('RoleSid', $roleSid)
            );

        $_resHandler = $this->responseHandler()->type(ServiceConversationParticipant::class)->returnApiResponse();

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Retrieve a list of all participants of the conversation
     *
     * @param string $chatServiceSid The SID of the [Conversation
     *        Service](https://www.twilio.com/docs/conversations/api/service-resource) the
     *        Participant resource is associated with.
     * @param string $conversationSid The unique ID of the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for participants.
     * @param int|null $pageSize How many resources to return in each list page. The default is 50,
     *        and the maximum is 100.
     * @param int|null $page The page index. This value is simply for client state.
     * @param string|null $pageToken The page token. This is provided by the API.
     *
     * @return ApiResponse Response from the API call
     */
    public function listServiceConversationParticipant(
        string $chatServiceSid,
        string $conversationSid,
        ?int $pageSize = null,
        ?int $page = null,
        ?string $pageToken = null
    ): ApiResponse {
        $_reqBuilder = $this->requestBuilder(
            RequestMethod::GET,
            '/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants'
        )
            ->server(Server::DEFAULT2)
            ->auth('accountSid_authToken')
            ->parameters(
                TemplateParam::init('ChatServiceSid', $chatServiceSid)->required(),
                TemplateParam::init('ConversationSid', $conversationSid)->required(),
                QueryParam::init('PageSize', $pageSize),
                QueryParam::init('Page', $page),
                QueryParam::init('PageToken', $pageToken)
            );

        $_resHandler = $this->responseHandler()
            ->type(ListServiceConversationParticipantResponse::class)
            ->returnApiResponse();

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Update an existing participant in the conversation
     *
     * @param string $chatServiceSid The SID of the [Conversation
     *        Service](https://www.twilio.com/docs/conversations/api/service-resource) the
     *        Participant resource is associated with.
     * @param string $conversationSid The unique ID of the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for this participant.
     * @param string $sid A 34 character string that uniquely identifies this resource.
     * @param string|null $xTwilioWebhookEnabled The X-Twilio-Webhook-Enabled HTTP request header
     * @param \DateTime|null $dateCreated The date on which this resource was created.
     * @param \DateTime|null $dateUpdated The date on which this resource was last updated.
     * @param string|null $identity A unique string identifier for the conversation participant as
     *        [Conversation User](https://www.twilio.com/docs/conversations/api/user-resource).
     *        This parameter is non-null if (and only if) the participant is using the
     *        [Conversation SDK](https://www.twilio.com/docs/conversations/sdk-overview) to
     *        communicate. Limited to 256 characters.
     * @param string|null $attributes An optional string metadata field you can use to store any
     *        data you wish. The string value must contain structurally valid JSON if specified.
     *        **Note** that if the attributes are not set `{}` will be returned.
     * @param string|null $roleSid The SID of a conversation-level
     *        [Role](https://www.twilio.com/docs/conversations/api/role-resource) to assign to the
     *        participant.
     * @param string|null $messagingBindingProxyAddress The address of the Twilio phone number that
     *        the participant is in contact with. 'null' value will remove it.
     * @param string|null $messagingBindingProjectedAddress The address of the Twilio phone number
     *        that is used in Group MMS. 'null' value will remove it.
     * @param int|null $lastReadMessageIndex Index of last “read” message in the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for the Participant.
     * @param string|null $lastReadTimestamp Timestamp of last “read” message in the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for the Participant.
     *
     * @return ApiResponse Response from the API call
     */
    public function updateServiceConversationParticipant(
        string $chatServiceSid,
        string $conversationSid,
        string $sid,
        ?string $xTwilioWebhookEnabled = null,
        ?\DateTime $dateCreated = null,
        ?\DateTime $dateUpdated = null,
        ?string $identity = null,
        ?string $attributes = null,
        ?string $roleSid = null,
        ?string $messagingBindingProxyAddress = null,
        ?string $messagingBindingProjectedAddress = null,
        ?int $lastReadMessageIndex = null,
        ?string $lastReadTimestamp = null
    ): ApiResponse {
        $_reqBuilder = $this->requestBuilder(
            RequestMethod::POST,
            '/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants/{Sid}'
        )
            ->server(Server::DEFAULT2)
            ->auth('accountSid_authToken')
            ->parameters(
                TemplateParam::init('ChatServiceSid', $chatServiceSid)->required(),
                TemplateParam::init('ConversationSid', $conversationSid)->required(),
                TemplateParam::init('Sid', $sid)->required(),
                HeaderParam::init('Content-Type', 'application/x-www-form-urlencoded'),
                HeaderParam::init('X-Twilio-Webhook-Enabled', $xTwilioWebhookEnabled)
                    ->serializeBy([ChannelWebhookEnabledType::class, 'checkValue']),
                FormParam::init('DateCreated', $dateCreated)
                    ->serializeBy([DateTimeHelper::class, 'toRfc3339DateTime']),
                FormParam::init('DateUpdated', $dateUpdated)
                    ->serializeBy([DateTimeHelper::class, 'toRfc3339DateTime']),
                FormParam::init('Identity', $identity),
                FormParam::init('Attributes', $attributes),
                FormParam::init('RoleSid', $roleSid),
                FormParam::init('MessagingBinding.ProxyAddress', $messagingBindingProxyAddress),
                FormParam::init('MessagingBinding.ProjectedAddress', $messagingBindingProjectedAddress),
                FormParam::init('LastReadMessageIndex', $lastReadMessageIndex),
                FormParam::init('LastReadTimestamp', $lastReadTimestamp)
            );

        $_resHandler = $this->responseHandler()->type(ServiceConversationParticipant::class)->returnApiResponse();

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Remove a participant from the conversation
     *
     * @param string $chatServiceSid The SID of the [Conversation
     *        Service](https://www.twilio.com/docs/conversations/api/service-resource) the
     *        Participant resource is associated with.
     * @param string $conversationSid The unique ID of the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for this participant.
     * @param string $sid A 34 character string that uniquely identifies this resource.
     * @param string|null $xTwilioWebhookEnabled The X-Twilio-Webhook-Enabled HTTP request header
     *
     * @return ApiResponse Response from the API call
     */
    public function deleteServiceConversationParticipant(
        string $chatServiceSid,
        string $conversationSid,
        string $sid,
        ?string $xTwilioWebhookEnabled = null
    ): ApiResponse {
        $_reqBuilder = $this->requestBuilder(
            RequestMethod::DELETE,
            '/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants/{Sid}'
        )
            ->server(Server::DEFAULT2)
            ->auth('accountSid_authToken')
            ->parameters(
                TemplateParam::init('ChatServiceSid', $chatServiceSid)->required(),
                TemplateParam::init('ConversationSid', $conversationSid)->required(),
                TemplateParam::init('Sid', $sid)->required(),
                HeaderParam::init('X-Twilio-Webhook-Enabled', $xTwilioWebhookEnabled)
                    ->serializeBy([ChannelWebhookEnabledType::class, 'checkValue'])
            );

        $_resHandler = $this->responseHandler()->returnApiResponse();

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Fetch a participant of the conversation
     *
     * @param string $chatServiceSid The SID of the [Conversation
     *        Service](https://www.twilio.com/docs/conversations/api/service-resource) the
     *        Participant resource is associated with.
     * @param string $conversationSid The unique ID of the
     *        [Conversation](https://www.twilio.com/docs/conversations/api/conversation-resource)
     *        for this participant.
     * @param string $sid A 34 character string that uniquely identifies this resource.
     *        Alternatively, you can pass a Participant's `identity` rather than the SID.
     *
     * @return ApiResponse Response from the API call
     */
    public function fetchServiceConversationParticipant(
        string $chatServiceSid,
        string $conversationSid,
        string $sid
    ): ApiResponse {
        $_reqBuilder = $this->requestBuilder(
            RequestMethod::GET,
            '/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants/{Sid}'
        )
            ->server(Server::DEFAULT2)
            ->auth('accountSid_authToken')
            ->parameters(
                TemplateParam::init('ChatServiceSid', $chatServiceSid)->required(),
                TemplateParam::init('ConversationSid', $conversationSid)->required(),
                TemplateParam::init('Sid', $sid)->required()
            );

        $_resHandler = $this->responseHandler()->type(ServiceConversationParticipant::class)->returnApiResponse();

        return $this->execute($_reqBuilder, $_resHandler);
    }
}

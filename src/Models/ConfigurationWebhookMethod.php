<?php

declare(strict_types=1);

/*
 * TwilioApIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace TwilioApIsLib\Models;

use Core\Utils\CoreHelper;
use Exception;
use stdClass;

/**
 * The HTTP method to be used when sending a webhook request., The HTTP method we should use to call
 * `conference_recording_status_callback`. Can be: `GET` or `POST` and defaults to `POST`., The HTTP
 * method we should use to call `conference_status_callback`. Can be: `GET` or `POST` and defaults to
 * `POST`., The HTTP method we should use when we call `recording_status_callback`. Can be: `GET` or
 * `POST` and defaults to `POST`., The HTTP method we should use to call `status_callback`. Can be:
 * `POST` or `GET` and the default is `POST`., The HTTP method we should use to call `wait_url`. Can be
 * `GET` or `POST` and the default is `POST`. When using a static audio file, this should be `GET` so
 * that we can cache the file., The method to be used when calling the webhook's URL.
 */
class ConfigurationWebhookMethod
{
    public const GET = 'GET';

    public const POST = 'POST';

    private const _ALL_VALUES = [self::GET, self::POST];

    /**
     * Ensures that all the given values are present in this Enum.
     *
     * @param array|stdClass|null|string $value Value or a list/map of values to be checked
     *
     * @return array|null|string Input value(s), if all are a part of this Enum
     *
     * @throws Exception Throws exception if any given value is not in this Enum
     */
    public static function checkValue($value)
    {
        $value = json_decode(json_encode($value), true); // converts stdClass into array
        if (CoreHelper::checkValueOrValuesInList($value, self::_ALL_VALUES)) {
            return $value;
        }
        throw new Exception("$value is invalid for ConfigurationWebhookMethod.");
    }
}

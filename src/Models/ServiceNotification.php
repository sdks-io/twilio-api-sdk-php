<?php

declare(strict_types=1);

/*
 * TwilioApIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace TwilioApIsLib\Models;

use stdClass;
use TwilioApIsLib\ApiHelper;

class ServiceNotification implements \JsonSerializable
{
    /**
     * @var array
     */
    private $accountSid = [];

    /**
     * @var array
     */
    private $chatServiceSid = [];

    /**
     * @var array
     */
    private $newMessage = [];

    /**
     * @var array
     */
    private $addedToConversation = [];

    /**
     * @var array
     */
    private $removedFromConversation = [];

    /**
     * @var array
     */
    private $logEnabled = [];

    /**
     * @var array
     */
    private $url = [];

    /**
     * Returns Account Sid.
     * The unique ID of the [Account](https://www.twilio.com/docs/iam/api/account) responsible for this
     * configuration.
     */
    public function getAccountSid(): ?string
    {
        if (count($this->accountSid) == 0) {
            return null;
        }
        return $this->accountSid['value'];
    }

    /**
     * Sets Account Sid.
     * The unique ID of the [Account](https://www.twilio.com/docs/iam/api/account) responsible for this
     * configuration.
     *
     * @maps account_sid
     */
    public function setAccountSid(?string $accountSid): void
    {
        $this->accountSid['value'] = $accountSid;
    }

    /**
     * Unsets Account Sid.
     * The unique ID of the [Account](https://www.twilio.com/docs/iam/api/account) responsible for this
     * configuration.
     */
    public function unsetAccountSid(): void
    {
        $this->accountSid = [];
    }

    /**
     * Returns Chat Service Sid.
     * The SID of the [Conversation Service](https://www.twilio.com/docs/conversations/api/service-
     * resource) the Configuration applies to.
     */
    public function getChatServiceSid(): ?string
    {
        if (count($this->chatServiceSid) == 0) {
            return null;
        }
        return $this->chatServiceSid['value'];
    }

    /**
     * Sets Chat Service Sid.
     * The SID of the [Conversation Service](https://www.twilio.com/docs/conversations/api/service-
     * resource) the Configuration applies to.
     *
     * @maps chat_service_sid
     */
    public function setChatServiceSid(?string $chatServiceSid): void
    {
        $this->chatServiceSid['value'] = $chatServiceSid;
    }

    /**
     * Unsets Chat Service Sid.
     * The SID of the [Conversation Service](https://www.twilio.com/docs/conversations/api/service-
     * resource) the Configuration applies to.
     */
    public function unsetChatServiceSid(): void
    {
        $this->chatServiceSid = [];
    }

    /**
     * Returns New Message.
     * The Push Notification configuration for New Messages.
     */
    public function getNewMessage(): ?array
    {
        if (count($this->newMessage) == 0) {
            return null;
        }
        return $this->newMessage['value'];
    }

    /**
     * Sets New Message.
     * The Push Notification configuration for New Messages.
     *
     * @maps new_message
     */
    public function setNewMessage(?array $newMessage): void
    {
        $this->newMessage['value'] = $newMessage;
    }

    /**
     * Unsets New Message.
     * The Push Notification configuration for New Messages.
     */
    public function unsetNewMessage(): void
    {
        $this->newMessage = [];
    }

    /**
     * Returns Added to Conversation.
     * The Push Notification configuration for being added to a Conversation.
     */
    public function getAddedToConversation(): ?array
    {
        if (count($this->addedToConversation) == 0) {
            return null;
        }
        return $this->addedToConversation['value'];
    }

    /**
     * Sets Added to Conversation.
     * The Push Notification configuration for being added to a Conversation.
     *
     * @maps added_to_conversation
     */
    public function setAddedToConversation(?array $addedToConversation): void
    {
        $this->addedToConversation['value'] = $addedToConversation;
    }

    /**
     * Unsets Added to Conversation.
     * The Push Notification configuration for being added to a Conversation.
     */
    public function unsetAddedToConversation(): void
    {
        $this->addedToConversation = [];
    }

    /**
     * Returns Removed From Conversation.
     * The Push Notification configuration for being removed from a Conversation.
     */
    public function getRemovedFromConversation(): ?array
    {
        if (count($this->removedFromConversation) == 0) {
            return null;
        }
        return $this->removedFromConversation['value'];
    }

    /**
     * Sets Removed From Conversation.
     * The Push Notification configuration for being removed from a Conversation.
     *
     * @maps removed_from_conversation
     */
    public function setRemovedFromConversation(?array $removedFromConversation): void
    {
        $this->removedFromConversation['value'] = $removedFromConversation;
    }

    /**
     * Unsets Removed From Conversation.
     * The Push Notification configuration for being removed from a Conversation.
     */
    public function unsetRemovedFromConversation(): void
    {
        $this->removedFromConversation = [];
    }

    /**
     * Returns Log Enabled.
     * Weather the notification logging is enabled.
     */
    public function getLogEnabled(): ?bool
    {
        if (count($this->logEnabled) == 0) {
            return null;
        }
        return $this->logEnabled['value'];
    }

    /**
     * Sets Log Enabled.
     * Weather the notification logging is enabled.
     *
     * @maps log_enabled
     */
    public function setLogEnabled(?bool $logEnabled): void
    {
        $this->logEnabled['value'] = $logEnabled;
    }

    /**
     * Unsets Log Enabled.
     * Weather the notification logging is enabled.
     */
    public function unsetLogEnabled(): void
    {
        $this->logEnabled = [];
    }

    /**
     * Returns Url.
     * An absolute API resource URL for this configuration.
     */
    public function getUrl(): ?string
    {
        if (count($this->url) == 0) {
            return null;
        }
        return $this->url['value'];
    }

    /**
     * Sets Url.
     * An absolute API resource URL for this configuration.
     *
     * @maps url
     */
    public function setUrl(?string $url): void
    {
        $this->url['value'] = $url;
    }

    /**
     * Unsets Url.
     * An absolute API resource URL for this configuration.
     */
    public function unsetUrl(): void
    {
        $this->url = [];
    }

    /**
     * Converts the ServiceNotification object to a human-readable string representation.
     *
     * @return string The string representation of the ServiceNotification object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'ServiceNotification',
            [
                'accountSid' => $this->getAccountSid(),
                'chatServiceSid' => $this->getChatServiceSid(),
                'newMessage' => $this->getNewMessage(),
                'addedToConversation' => $this->getAddedToConversation(),
                'removedFromConversation' => $this->getRemovedFromConversation(),
                'logEnabled' => $this->getLogEnabled(),
                'url' => $this->getUrl(),
                'additionalProperties' => $this->additionalProperties
            ]
        );
    }

    protected $propertyNames = [
        'account_sid',
        'chat_service_sid',
        'new_message',
        'added_to_conversation',
        'removed_from_conversation',
        'log_enabled',
        'url'
    ];

    private $additionalProperties = [];

    /**
     * Add an additional property to this model.
     *
     * @param string $name Name of property.
     * @param mixed $value Value of property.
     */
    public function addAdditionalProperty(string $name, $value)
    {
        if (in_array($name, $this->propertyNames, true)) {
            throw new \InvalidArgumentException(
                "The additional property key, '$name' conflicts with one of the model's properties"
            );
        }

        $this->additionalProperties[$name] = $value;
    }

    /**
     * Find an additional property by name in this model or false if property does not exist.
     *
     * @param string $name Name of property.
     *
     * @return mixed|false Value of the property.
     */
    public function findAdditionalProperty(string $name)
    {
        if (isset($this->additionalProperties[$name])) {
            return $this->additionalProperties[$name];
        }
        return false;
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (!empty($this->accountSid)) {
            $json['account_sid']               = $this->accountSid['value'];
        }
        if (!empty($this->chatServiceSid)) {
            $json['chat_service_sid']          = $this->chatServiceSid['value'];
        }
        if (!empty($this->newMessage)) {
            $json['new_message']               = $this->newMessage['value'];
        }
        if (!empty($this->addedToConversation)) {
            $json['added_to_conversation']     = $this->addedToConversation['value'];
        }
        if (!empty($this->removedFromConversation)) {
            $json['removed_from_conversation'] = $this->removedFromConversation['value'];
        }
        if (!empty($this->logEnabled)) {
            $json['log_enabled']               = $this->logEnabled['value'];
        }
        if (!empty($this->url)) {
            $json['url']                       = $this->url['value'];
        }
        $json = array_merge($json, $this->additionalProperties);

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}

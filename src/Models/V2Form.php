<?php

declare(strict_types=1);

/*
 * TwilioApIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace TwilioApIsLib\Models;

use stdClass;
use TwilioApIsLib\ApiHelper;

class V2Form implements \JsonSerializable
{
    /**
     * @var string|null
     */
    private $formType;

    /**
     * @var array
     */
    private $forms = [];

    /**
     * @var array
     */
    private $formMeta = [];

    /**
     * @var array
     */
    private $url = [];

    /**
     * Returns Form Type.
     * The Type of this Form. Currently only `form-push` is supported.
     */
    public function getFormType(): ?string
    {
        return $this->formType;
    }

    /**
     * Sets Form Type.
     * The Type of this Form. Currently only `form-push` is supported.
     *
     * @maps form_type
     * @factory \TwilioApIsLib\Models\FormEnumFormTypes::checkValue
     */
    public function setFormType(?string $formType): void
    {
        $this->formType = $formType;
    }

    /**
     * Returns Forms.
     * Object that contains the available forms for this type. This available forms are given in the
     * standard [JSON Schema](https://json-schema.org/) format
     */
    public function getForms(): ?array
    {
        if (count($this->forms) == 0) {
            return null;
        }
        return $this->forms['value'];
    }

    /**
     * Sets Forms.
     * Object that contains the available forms for this type. This available forms are given in the
     * standard [JSON Schema](https://json-schema.org/) format
     *
     * @maps forms
     */
    public function setForms(?array $forms): void
    {
        $this->forms['value'] = $forms;
    }

    /**
     * Unsets Forms.
     * Object that contains the available forms for this type. This available forms are given in the
     * standard [JSON Schema](https://json-schema.org/) format
     */
    public function unsetForms(): void
    {
        $this->forms = [];
    }

    /**
     * Returns Form Meta.
     * Additional information for the available forms for this type. E.g. The separator string used for
     * `binding` in a Factor push.
     */
    public function getFormMeta(): ?array
    {
        if (count($this->formMeta) == 0) {
            return null;
        }
        return $this->formMeta['value'];
    }

    /**
     * Sets Form Meta.
     * Additional information for the available forms for this type. E.g. The separator string used for
     * `binding` in a Factor push.
     *
     * @maps form_meta
     */
    public function setFormMeta(?array $formMeta): void
    {
        $this->formMeta['value'] = $formMeta;
    }

    /**
     * Unsets Form Meta.
     * Additional information for the available forms for this type. E.g. The separator string used for
     * `binding` in a Factor push.
     */
    public function unsetFormMeta(): void
    {
        $this->formMeta = [];
    }

    /**
     * Returns Url.
     * The URL to access the forms for this type.
     */
    public function getUrl(): ?string
    {
        if (count($this->url) == 0) {
            return null;
        }
        return $this->url['value'];
    }

    /**
     * Sets Url.
     * The URL to access the forms for this type.
     *
     * @maps url
     */
    public function setUrl(?string $url): void
    {
        $this->url['value'] = $url;
    }

    /**
     * Unsets Url.
     * The URL to access the forms for this type.
     */
    public function unsetUrl(): void
    {
        $this->url = [];
    }

    /**
     * Converts the V2Form object to a human-readable string representation.
     *
     * @return string The string representation of the V2Form object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'V2Form',
            [
                'formType' => $this->formType,
                'forms' => $this->getForms(),
                'formMeta' => $this->getFormMeta(),
                'url' => $this->getUrl(),
                'additionalProperties' => $this->additionalProperties
            ]
        );
    }

    protected $propertyNames = ['form_type', 'forms', 'form_meta', 'url'];

    private $additionalProperties = [];

    /**
     * Add an additional property to this model.
     *
     * @param string $name Name of property.
     * @param mixed $value Value of property.
     */
    public function addAdditionalProperty(string $name, $value)
    {
        if (in_array($name, $this->propertyNames, true)) {
            throw new \InvalidArgumentException(
                "The additional property key, '$name' conflicts with one of the model's properties"
            );
        }

        $this->additionalProperties[$name] = $value;
    }

    /**
     * Find an additional property by name in this model or false if property does not exist.
     *
     * @param string $name Name of property.
     *
     * @return mixed|false Value of the property.
     */
    public function findAdditionalProperty(string $name)
    {
        if (isset($this->additionalProperties[$name])) {
            return $this->additionalProperties[$name];
        }
        return false;
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->formType)) {
            $json['form_type'] = FormEnumFormTypes::checkValue($this->formType);
        }
        if (!empty($this->forms)) {
            $json['forms']     = $this->forms['value'];
        }
        if (!empty($this->formMeta)) {
            $json['form_meta'] = $this->formMeta['value'];
        }
        if (!empty($this->url)) {
            $json['url']       = $this->url['value'];
        }
        $json = array_merge($json, $this->additionalProperties);

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
